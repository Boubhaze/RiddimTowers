// This service is currently disabled to prevent Vercel build issues with process.env
// and to remove API dependencies as requested.

export const getDubLesson = async (topic: string): Promise<string> => {
    return "Tutorials are currently disabled.";
};
